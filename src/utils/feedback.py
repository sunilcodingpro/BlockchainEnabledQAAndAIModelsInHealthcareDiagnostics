def store_feedback(rule_id, feedback_text):
    """
    Stores user feedback for a specific rule.
    Args:
        rule_id: ID of the rule the feedback concerns.
        feedback_text: User-provided feedback string.
    Returns:
        None
    """
    # Placeholder: In production, save feedback in a secure database or log system
    print(f"Feedback for {rule_id}: {feedback_text}")